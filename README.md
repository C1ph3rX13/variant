# variant

Golang Malware Framework

## Update

### 20250919

1. Windows API 支持完全更新为 [xwindows](https://github.com/C1ph3rX13/xwindows)

2. `crypto` 加密模块返回值统一更新，修复了失效的加密
3. `loader` 加载模块优化，新增了其他加载模式
4. `payload` 载荷模块完全重构，支持智能判断加密方式
5. `remote` 网络请求模块优化仿真，支持多种随机客户端模拟
6. `templates` 模板渲染模块全面重构，同时支持函数式和快捷配置

